Great, now 2 things:
•⁠  ⁠faker script, it fills all the non authentication related 
tables with fake data (let’s do ~5k rows each)
•⁠  ⁠⁠ETL, pulls raw data from the api db tables, formats it and saves
 it to a new db with new structure, relevant for analysis